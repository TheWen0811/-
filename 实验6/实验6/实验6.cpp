//假设 顺序表 L中的元素按从小到大的次序排列，设计算法以删除表中重复的元素, 并要求时间尽可能少。
//要求：
//对顺序表（1, 1, 2, 2, 2, 3, 4, 5, 5, 5, 6, 6, 7, 7, 8, 8, 8, 9）模拟执行本算法，并统计移动元素的次数。
#include<iostream>
using namespace std;
int main() {
	int b[9];
	int a[] = { 1, 1, 2, 2, 2, 3, 4, 5, 5, 5, 6, 6, 7, 7, 8, 8, 8, 9 };
	cout << 1 << " ";
	for (int i = 0; i < 17; i++) {
		if (a[i] != a[i + 1])cout<<a[i + 1]<<" ";
	}
}